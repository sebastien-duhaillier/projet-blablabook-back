# Variables d'environnement pour Railway
# Railway configurera automatiquement ces variables

# Port (automatiquement configuré par Railway)
PORT=$PORT

# Base de données PostgreSQL (Railway la créera automatiquement)
# Railway fournira automatiquement DATABASE_URL
DB_URL=$DATABASE_URL

# JWT Secret - IMPORTANT: À configurer manuellement dans Railway
JWT_SECRET=your_super_secure_jwt_secret_key_here_change_this

# Environnement
NODE_ENV=production

# CORS Origin - URL de votre frontend (à configurer plus tard)
FRONTEND_URL=https://your-frontend-domain.com
